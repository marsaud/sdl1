#ifndef ZONESETLOADER_H
#define ZONESETLOADER_H

#include <fstream>
#include <string>
#include <sstream>
#include <vector>

class ZoneSetLoader
{
    public:
        ZoneSetLoader();
        virtual ~ZoneSetLoader();

        void load();
    protected:
    private:
};

#endif // ZONESETLOADER_H
